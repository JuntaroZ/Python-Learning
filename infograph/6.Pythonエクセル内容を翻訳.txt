・以下内容を初心者もわかるようインフォグラフィックスを作成
・タイトル：Python でエクセル内容を翻訳してみよう！
・A1に「日本語」、B1に「英語」が入っています
・A2, B2以降は以下内容にしてください。
　・おはようございます。	Good morning.
　・今日はとてもいい天気です。	It is very nice weather today.
　・私は毎朝コーヒーを飲みます。	I drink coffee every morning.
・A4サイズで作成すること
・文字化けを修正してください
・ページ番号６
・プログラム部分の下地は白一色にすること。

・変数は白色、予約語とメソッドは薄紫色、大文字の定数は青色、カッコは橙色、文字列は水色、コメントは灰色、import/fromは紫色、ライブラリ名は薄青緑色
・出力ログはすべて白色
・ダブルコートは半角で水色
　┗色の説明はすべて不要です

・左側にプログラム欄を生成。縮小してもよいのでプログラムは最終行まですべて出力
・右側に1. ～ 7. の説明欄を生成。
・1. ～ 7.の説明欄はそれぞれ独立させること。
・1. ～ 7.の説明欄の番号の色はすべて橙色固定にすること。
・1. ～ 7.の説明欄からそれぞれ1. ～ 7.のプログラム箇所への矢印は不要！
・1. ～ 7.の説明欄は説明だけ記載して答えを書かないこと。
・3. の説明文は「翻訳元言語を指定する」
・3. の説明文は「翻訳先言語を指定する」
・ヒントの欄は不要で、代わりに期待する実行結果の簡単な説明を記載すること。
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
import openpyxl
from google import genai
from itertools import count

# エクセルを読み込む
book = openpyxl.load_workbook("./excel/translate.xlsx")
sheet = book.active

SOURCE_LANG = 1._______________ # 翻訳元言語をシートA1から取得
TARGET_LANG = 2._______________ # 翻訳先言語をシートB1から取得

print("翻訳元言語：" + SOURCE_LANG  + "、翻訳先言語：" + TARGET_LANG)

client = genai.Client()
chat = client.chats.create(model="gemini-3.5-flash-lite")

for index in count(2): # 無限ループ
    # A列の文章を読み取る
    translate_text = sheet[f"A{index}"].value
    # A列が空白の場合はループを抜ける
    if translate_text is None or translate_text == "":
        break
    # 翻訳を実行する
    response = chat.send_message(
        3._________ + "を" + 4._________ + 
        "に翻訳（翻訳結果を１つ返してください）：" + 5.______________
    )
    # B列の各行に結果(response.text)を書き込む
    6.______________________ = 7.___________
    
book.save("./excel/translate.xlsx")
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
